# LocalStack auth token — get from app.localstack.cloud
auth_token = "ls-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# AWS resource names
sqs_name    = "log-processing"
bucket_name = "log-processing"

# AWS config
aws_region = "us-east-1"
